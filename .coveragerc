[report]
# Regexes for lines to exclude from consideration
exclude_lines =
    # Don't complain abstract classes:
    ...

    # Don't complain if tests don't hit defensive assertion code:
    raise NotImplementedError
    raise ValueError
    raise TypeError
    raise RuntimeError
    msg = .*
    raise Exception(msg)
